name: Workflow
on:
  push:
    branches:
      - QA
  schedule:
      - cron: '00 15 * * *'
jobs:
  simple_test:
    runs-on: ubuntu-latest
    steps:
      - name: bonjour
        run: echo "Hello world"
